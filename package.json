{
  "name": "edulink-interface",
  "version": "1.0.0",
  "description": "Interface EduLink avec Tailwind CSS et SASS",
  "scripts": {
    "sass": "npx sass src/styles/main.scss dist/temp.css",
    "tailwind": "npx tailwindcss -i ./dist/temp.css -o ./dist/output.css",
    "dev": "npm run sass && npm run tailwind -- --watch",
    "build": "npm run sass && npm run tailwind -- --minify"
  },
  "keywords": ["tailwind", "sass", "edulink"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "sass": "^1.69.5",
    "tailwindcss": "^3.4.0"
  }
}